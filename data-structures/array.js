// Arrays
// Ordered lists!
// When to use
    // You need order
    // When you need fast access/insertion/removal... sort of

// If you have an index it is constant lookup time (if the index is within the range of the array)
// Inserting/removing at the end of the array is constant time
// Inserting/removing at the beginning of the array is linear time - We have to reindex each element in the array


// Big O
// -----
// Insertion - 
    // End - O(1)
    // Beginning - O(n)
// Removal - 
    // End - O(1)
    // Beginning - O(n)
// Searching - O(n)
// Access - O(1)


// Array methods
    // push - O(1)
    // pop - O(1)
    // shift - O(n)
    // unshift - O(n)
    // sort - O(nlogn)
    // forEach/map/filter/reduce - O(n)
    // concat/slice/splice - O(n)


